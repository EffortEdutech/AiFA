# Copy to .env.local for local dev. In Vercel, set these under Project
# Settings -> Environment Variables instead (Production + Preview).
# These are the anon/publishable key -- safe to expose to the browser --
# not the service_role key used by supabase/functions/public-homepage.
NEXT_PUBLIC_SUPABASE_URL=https://yotapuotkbyyocraraza.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key-here
