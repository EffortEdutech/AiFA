/**
 * SUPERSEDED (Sprint 6) — this test file was renamed to
 * capturePipeline.test.ts when the Expense-only pipeline was generalised
 * to also cover Sale and Purchase (Vol 6_0 §4). This file could not be
 * deleted from this sandbox (the mounted project folder only supports
 * file creation, not deletion/rename), so it is left as a trivial
 * pass-through instead of stale, broken test code.
 *
 * See capturePipeline.test.ts for the real Expense-domain tests and
 * salesPurchasePipeline.test.ts for Sale/Purchase.
 */
describe("expensePipeline.test.ts (superseded)", () => {
  it("has moved to capturePipeline.test.ts — see that file", () => {
    expect(true).toBe(true);
  });
});
