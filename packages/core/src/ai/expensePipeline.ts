/**
 * SUPERSEDED (Sprint 6) — this module was renamed to capturePipeline.ts
 * when the Expense-only pipeline was generalised to also cover Sale and
 * Purchase (Vol 6_0 §4). This file could not be deleted from this sandbox
 * (the mounted project folder only supports file creation, not
 * deletion/rename — see app/README.md's sync-workflow notes), so it is
 * left as an empty re-export shim instead of stale, broken source.
 *
 * Do not add new code here. Import from "@/ai/capturePipeline" instead.
 */
export * from "./capturePipeline";
